spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_class
    username: root
    password: kkhj7346
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # ?? ????? update, ?? ????? validate ?? none ??
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
# ?? ?? ??
server:
  port: 8080


